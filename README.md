# Basketball Team Stats Tool


This project reads from  the "constants" data (`PLAYERS` and `TEAMS`) in `constants.py`. The data will be transalated to a collection of data and to be divided into 3 teams. Then the teams are displayed based on which team info you want to see. 

